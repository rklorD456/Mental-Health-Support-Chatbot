# templates/

Jinja2 HTML templates served by FastAPI.

## Files

| File | Purpose |
|------|---------|
| `index.html` | The chat interface — header, message list, typing indicator, and input area. Loads styles from `/static/css/style.css` and logic from `/static/js/script.js`. |
