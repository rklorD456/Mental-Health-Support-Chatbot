# static/

Static frontend assets served by FastAPI at `/static/`.

## Structure

```
static/
├── css/
│   └── style.css    # Chat UI styles (Inter font, responsive layout, typing animation)
└── js/
    └── script.js    # Chat logic (send messages, manage sessions, show typing indicator)
```
